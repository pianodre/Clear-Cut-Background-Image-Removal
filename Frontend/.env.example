# Copy to .env and fill in.  cp .env.example .env
# Only VITE_* vars are exposed to the browser. Never put secrets here.

VITE_SUPABASE_URL=https://pcpmbdxfquimdddjyvuq.supabase.co
VITE_SUPABASE_ANON_KEY=        # Supabase publishable (anon) key — safe to ship

# Backend API base URL. Blank in dev (uses the Vite proxy to /api).
# In production set the deployed backend origin.
VITE_API_BASE_URL=
